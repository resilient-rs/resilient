[package]
name = "resilient"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2.0.18"
tokio = { version = "1", features = ["time"] }
futures-util = { version = "0.3", default-features = false, features = ["std"] }
